# Technomantra Fast Coding Engine V4.13.0

Normal tasks follow RESOLVE -> READ SMALL -> EDIT -> VERIFY. Native retrieval supplies at most 2–4 ranked source chunks before the first provider call. Reuse current evidence, make one focused edit batch, verify once, and continue follow-up work in the same project session. A framework preview is durable project infrastructure: reuse it and never run it as a foreground task-owned command. Deep Build, Visual Build, backend, bug-fix and exact-edit details are injected only when the current request needs them.
